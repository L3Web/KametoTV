{% extends "base.html.twig" %}
    {% block title %}Panier !{% endblock %}

    {% block body %}
        {% if items | length > 0%}
            <table>
                <thead>
                    <tr>
                        <th>Produit</th>
                        <th>Prix</th>
                        <th>Quantité</th>
                        <th>Total</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                        <tr>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p>Malheureusement votre Panier est vide ...</p>
        {% endif %}

    {% endblock  %}